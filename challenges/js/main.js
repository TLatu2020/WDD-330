import ToDos from './ToDos.js';

const toDo = new ToDos("#list", "myList");

toDo.listTodos();

WebFont.load({
    google: {
        families: [
            'Roboto Slab', 'Montserrat'
        ]
    }
});